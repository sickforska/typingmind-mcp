{
  "name": "typingmind-mcp",
  "version": "1.0.0",
  "description": "TypingMind MCP Connector",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "mcp": "npx @typingmind/mcp@latest $MCP_AUTH_TOKEN"
  },
  "dependencies": {
    "@typingmind/mcp": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
